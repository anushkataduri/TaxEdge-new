spring:
  datasource:
    url: jdbc:postgresql://localhost:3306/taxedge
    username: postgres
    password: Srini123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    com.taxedge: DEBUG